# ─── Sanity CMS ───────────────────────────────────────────
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2025-01-01
SANITY_API_TOKEN=your_sanity_api_token

# ─── AI Chat (Infernape) ──────────────────────────────────
# At least one of these must be set for the chat feature
GEMINI_API_KEY=your_gemini_api_key
GOOGLE_API_KEY=your_gemini_api_key

# ─── Email (Contact Form via Resend) ─────────────────────
# Optional — if unset, messages are logged to console
RESEND_API_KEY=your_resend_api_key
CONTACT_EMAIL=your_email@example.com

# ─── Turnstile (CAPTCHA) ─────────────────────────────────
NEXT_PUBLIC_TURNSTILE_SITEKEY=your_turnstile_sitekey
TURNSTILE_SECRET_KEY=your_turnstile_secret

# ─── Site URL ────────────────────────────────────────────
# Override auto-detection for sitemap, OG images, etc.
# NEXT_PUBLIC_SITE_URL=https://yourdomain.com

# ─── Rate Limiting (Optional — falls back to in-memory) ──
# Sign up at https://upstash.com for free Redis
# UPSTASH_REDIS_REST_URL=https://your-region.upstash.io/redis/xxx
# UPSTASH_REDIS_REST_TOKEN=your_token

# ─── Auth (Reserved for future admin area) ───────────────
# SESSION_SECRET=your-secret-at-least-32-characters-long
